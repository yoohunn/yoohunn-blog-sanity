import blockContent from './blockContent'
import series from './series'
import post from './post'
import author from './author'
import tag from './tag'

export const schemaTypes = [post, author, series, blockContent, tag]
